const Loading = () => {
    
    const loading = {
        textAlign: 'center',
        margin: '20px',
        fontSize: '1.3em'
    }

    return (
        <div>
            <h1 style={loading} >Loading</h1>
        </div>
    )
}

export default Loading
